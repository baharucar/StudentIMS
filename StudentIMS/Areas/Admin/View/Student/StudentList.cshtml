@model List<Student>

<div class="container">

    <div class="row">
        <div class="col-6">
            <h3>Student List</h3>
        </div>
        <div class="col-6 align-items-center text-end">
            <a asp-controller="Student" asp-action="Create" class="w-50 btn btn-outline-primary">Create</a>
        </div>
    </div>   
        
   
    <table class="table table-striped table-hover" id="myTable">
        <thead>
            <tr>
                <th>Name</th>
                <th>Surname</th>
                <th>Student Number</th>
                <th>Age</th>
                @*<th>Department</th>*@
          
            </tr>
        </thead>
        @*<tbody>
            @foreach (var student in Model)
            {
                <tr>
                    <td>@student.Name</td>
                    <td>@student.Surname</td>
                    <td>@student.StudentNumber</td>
                    <td>@student.Age</td>
                    <td>@student.Department.Depart</td>
                    <td>
                        <div>
                            <a asp-controller="Student" asp-action="Edit" asp-route-id="@student.Id" class="btn btn-primary">Edit</a>
                            <a asp-controller="Student" asp-action="Delete" asp-route-id="@student.Id" class="btn btn-danger">Delete</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>*@
    </table>

</div>

@section Scripts{
    <script src="~/js/studentlist.js"></script>
}
```

